# 교실 퀴즈 사용 설명서

같은 와이파이에 붙은 학생들이 **QR로 접속**해 함께 푸는 교실 실시간 퀴즈 게임 서버입니다.
교사 PC에서 **exe 하나를 더블클릭**하면 끝 — 학생은 앱 설치 없이 **브라우저**로 들어옵니다.

- 빠른 시작이 궁금하면 → [2. 빠른 시작](#2-빠른-시작)
- 특정 기능만 보려면 → 아래 목차에서 해당 항목으로

---

## 목차

1. [소개](#1-소개)
2. [빠른 시작](#2-빠른-시작)
3. [화면 구성](#3-화면-구성)
4. [서버 실행](#4-서버-실행)
5. [퀴즈 만들기 (편집기)](#5-퀴즈-만들기-편집기)
6. [퀴즈 공유 — 가져오기 / 내보내기](#6-퀴즈-공유--가져오기--내보내기)
7. [게임 진행](#7-게임-진행)
8. [점수 규칙](#8-점수-규칙)
9. [결과 저장과 내보내기](#9-결과-저장과-내보내기)
10. [네트워크 (무선 / 유선)](#10-네트워크-무선--유선)
11. [데이터 저장과 백업](#11-데이터-저장과-백업)
12. [자주 묻는 질문](#12-자주-묻는-질문)
13. [AI로 퀴즈 만들기 (MCP)](#13-ai로-퀴즈-만들기-mcp)

---

## 1. 소개

- 교사 PC에서 **`classroom-quiz.exe` 더블클릭** → 서버가 켜지고 기본 브라우저에 **교사 화면**이 자동으로 열립니다.
- 학생은 교사 화면의 **QR을 찍어** 닉네임만 입력하면 입장합니다(앱 설치 없음).
- 진행은 **로비 → 문제 → 정답 공개 → 순위표 → 시상대** 순서. 교사가 화면에서 버튼으로 넘깁니다.
- 인터넷이 필요 없습니다 — 웹 화면·QR이 exe 안에 들어 있어 **오프라인 교실 와이파이(LAN)** 에서도 동작합니다.
- 배포물은 **exe 파일 하나**. 만든 퀴즈는 **JSON 파일 하나**(그림까지 내장)라 다른 교사와 쉽게 주고받습니다.

---

## 2. 빠른 시작

1. `classroom-quiz.exe`를 **더블클릭**합니다. → 교사 화면(`/host`)이 자동으로 열립니다.
2. (처음이면) 화면 위 **＋ 퀴즈 만들기**로 문제를 만들고 **저장**합니다. → [5. 퀴즈 만들기](#5-퀴즈-만들기-편집기)
3. 학생들에게 화면의 **QR**을 보여줍니다(TV/프로젝터). 멀면 **전체화면으로 크게** 버튼을 누르세요.
4. 학생이 QR을 찍고 **닉네임**을 넣으면 교사 화면에 실시간으로 뜹니다.
5. 퀴즈를 고르고 **시작하기** → **정답 공개 → 순위표 → 다음 문제 → … → 최종 결과**.
6. 끝나면 결과가 자동 저장됩니다. **📊 결과**에서 보고 **CSV로 내보내기** 할 수 있습니다.

> 실수로 시작했거나 처음으로 돌아가려면 → 화면 왼쪽 위 **엠블럼을 클릭**하세요.

---

## 3. 화면 구성

브라우저 주소(같은 PC 기준)로 구분됩니다. **편집기·결과는 교사 PC(본인)에서만** 열립니다.

| 화면 | 주소 | 용도 |
|---|---|---|
| **교사 화면** | `/host` | 로비·문제·순위·시상대 진행 (TV/프로젝터 표출) |
| **학생 화면** | `/` (QR 목적지) | 닉네임 입장 → 보기 선택 |
| **퀴즈 만들기** | `/author` | 퀴즈·문항·그림 편집 (교사 PC 전용) |
| **결과** | `/results` | 지난 게임 결과 보기·내보내기 (교사 PC 전용) |

- 화면 좌상단 **teaveloper 엠블럼**을 누르면 언제나 **처음(교사 메인)** 으로 돌아갑니다.
- 우상단 **🌙/☀️ 버튼**으로 라이트/다크 테마를 전환합니다(자동으로 기기 설정도 따름).
- 우상단 **[AI 연결]** 은 AI에게 퀴즈 만들기를 맡길 때 **한 번만** 누르면 됩니다.
  → [13. AI로 퀴즈 만들기](#13-ai로-퀴즈-만들기-mcp)

---

## 4. 서버 실행

- `classroom-quiz.exe`를 더블클릭하면 서버가 **모든 네트워크(0.0.0.0)** 에 바인딩됩니다 — 학생이 LAN으로 붙으려면 필수입니다.
- 기본 포트는 **8080**, 사용 중이면 다음 비어 있는 포트를 자동으로 잡습니다.
- 교사 화면에는 **연결 방식 배지**(무선 / 유선)가 표시됩니다. → [10. 네트워크](#10-네트워크-무선--유선)
- 콘솔 창이 보이는 빌드는 **창을 닫으면 서버가 종료**됩니다. 진단용 로그는 exe 옆 `classroom-quiz.log`에 남습니다.

> **방화벽**: 처음 실행 때 Windows가 네트워크 허용을 물으면 **허용**하세요(개인/Private 네트워크). 거부하면 학생이 접속하지 못합니다.

**새 버전으로 바꾸기**: 앱을 닫고, 내려받은 exe로 **파일만 덮어쓰면** 됩니다.
같은 폴더의 `quizzes/`·`results/` 는 건드리지 않으므로 **퀴즈와 기록은 그대로**이고,
[AI 연결](#13-ai로-퀴즈-만들기-mcp)도 앱을 켜는 순간 새 파일을 가리키도록 자동으로 맞춰집니다.
(내려받은 파일 이름에 버전·번호가 붙어 있어도 그대로 두면 됩니다.)

---

## 5. 퀴즈 만들기 (편집기)

**＋ 퀴즈 만들기**(`/author`)에서 만듭니다. 왼쪽은 퀴즈 목록, 오른쪽은 편집기입니다.

1. **＋ 새 퀴즈** → 위에 **퀴즈 제목**을 입력합니다.
2. 문항마다:
   - **질문**을 입력합니다.
   - (선택) **🖼 그림 추가** → 이미지를 고르면 자동으로 줄여 문항에 넣습니다. (제거도 가능)
   - **보기**를 2~6개 입력하고, 정답인 보기에 **정답** 체크(정답은 1개 이상).
   - **제한시간(초)** 과 **배점**을 정합니다(기본 20초 / 1000점).
   - **＋ 보기 추가**, **＋ 문항 추가**로 늘립니다.
3. **저장**을 누릅니다. (제목·문항·정답이 비어 있으면 안내가 뜹니다.)

- **문항 순서 바꾸기**: 문항 왼쪽 위 **⠿ 손잡이를 잡고 끌어서** 원하는 자리에 놓으면 됩니다.
  놓을 자리는 파란 선으로 표시됩니다. (질문 글을 마우스로 고르는 것과 섞이지 않도록, 손잡이를
  잡았을 때만 끌립니다.)
- **문항 복제**: **복제**를 누르면 그 문항이 바로 아래에 똑같이 하나 더 생깁니다. 비슷한 문항을
  여러 개 낼 때 숫자나 낱말만 바꾸면 되고, **원래 문항은 그대로 남습니다.**
- 보기 표시는 **teaveloper 테마 색 + 보기 순번만큼의 엠블럼**(1개·2개 나란히·3개부터 방사형)입니다.
  학생은 **보기 글**을 읽고 고르므로, 엠블럼은 화면을 꾸미는 장식입니다.
- **그림은 별도 파일이 아니라** 퀴즈 JSON 안에 통째로 저장됩니다 → [11. 데이터 저장](#11-데이터-저장과-백업)
- 문항을 손으로 다 쓰기 번거로우면 **AI에게 초안을 맡길 수 있습니다** → [13. AI로 퀴즈 만들기](#13-ai로-퀴즈-만들기-mcp)

---

## 6. 퀴즈 공유 — 가져오기 / 내보내기

퀴즈 한 개 = **JSON 파일 하나**(그림까지 내장)라 파일만 주고받으면 됩니다.

- **내보내기**: 편집기 위쪽 **↧ 내보내기** → `제목.json` 파일로 저장됩니다(그림 포함).
- **가져오기**: 왼쪽 **↥ 가져오기 (.json)** → 파일을 고르면 새 퀴즈로 목록에 추가됩니다.
  - 가져온 퀴즈는 **항상 새 ID**로 저장되어 **기존 퀴즈를 덮어쓰지 않습니다**.

> 다른 교사에게 줄 때는 내보낸 `.json` 하나만 건네면 됩니다. 받는 사람 PC에 그림 파일이 없어도 문제 그림이 그대로 나옵니다.

---

## 7. 게임 진행

교사 화면(`/host`)에서 진행합니다.

1. **로비** — QR·접속 주소·참가자 목록이 뜹니다. 퀴즈를 고르고 **시작하기**.
   - **전체화면으로 크게**(또는 QR 클릭): QR을 화면 가득 키웁니다. 옆에 **참가자 명단·인원**이 함께 떠서, 충분히 들어왔는지 보고 닫을 수 있습니다. (아무 곳이나 클릭 또는 **ESC** 로 닫기)
2. **문제** — 질문·보기·타이머가 표시됩니다. 연결된 학생이 **전원 답하면 즉시 공개**, 아니면 시간이 끝나면 공개. **정답 공개 →** 로 넘길 수도 있습니다.
   - 타이머는 **서버가 재는 남은 시간**입니다. 학생이 답할 때마다 화면이 갱신돼도 숫자가 되돌아가지 않고, 교사 화면과 학생 화면이 같은 시간을 가리킵니다.
3. **정답 공개** — 정답 표시 + 보기별 응답 분포 막대.
4. **순위표** — 누적 점수 순위 + 등수 변동(▲▼). **다음 문제 →**.
5. **시상대** — Top 3 단상 + 전체 순위. 학생 화면엔 본인 최종 등수가 뜹니다.
6. **처음으로 ↩** 또는 **엠블럼 클릭**으로 로비로 돌아갑니다.

> **재접속**: 교실 와이파이가 잠깐 끊겨도 **같은 점수로 자동 재접속**됩니다.
> 학생이 화면을 닫았거나 폰이 잠겨 끊겼을 때도, **QR을 다시 찍어 같은 닉네임으로 들어오면
> 하던 게임에 그대로 이어집니다**(점수 유지).
>
> **로비(게임 시작 전)** 에서 나간 학생은 참가자 명단에서 바로 사라집니다. 그래서 학생이
> 닉네임을 고쳐 다시 들어와도 **옛 이름이 남지 않습니다.**

---

## 8. 점수 규칙

- **속도 점수**: `배점 × (1 − f/2)`, `f = 응답시간 ÷ 제한시간`. → 빠를수록 만점, 막판에 맞히면 절반.
  타이밍은 **서버 기준**(문제 송출 시각 vs 답안 도착 시각)이라 학생 시계를 신뢰하지 않습니다.
- **연속 정답(스트릭) 보너스**: 연속으로 맞힐 때마다 **+100**, 최대 **+500**.
- 틀리거나 못 맞히면 스트릭은 0으로 초기화됩니다.

---

## 9. 결과 저장과 내보내기

게임을 **시상대까지 정상 종료**하면 결과가 자동으로 저장됩니다.

- **결과 화면**(`/results`): 퀴즈별로 묶여 **날짜순**으로 쌓입니다. 한 줄을 펼치면 **순위·이름·점수·정답수** 표가 나옵니다.
- **내보내기**:
  - **CSV** — 엑셀로 여는 형성평가 기록용(한글 깨짐 방지 처리됨).
  - **JSON** — 원본 그대로 보관·공유용.
- 필요 없으면 결과별 **삭제**도 가능합니다.

> 게임 중간에 엠블럼으로 로비에 돌아가면(미완료) 결과는 저장되지 않습니다.

---

## 10. 네트워크 (무선 / 유선)

교사 화면 헤더에 **학생 접속 주소가 어떤 연결인지** 배지로 표시됩니다.

- **무선 (Wi-Fi)** — 학생 무선망에 붙은 일반적인 상태.
- **유선 (랜선)** — 랜선으로 잡힌 상태. 학생 무선망을 직접 구성한 경우가 아니면, 학생이 **같은 와이파이**에 있는지 확인하세요.

설계 메모:
- 학생이 LAN으로 붙어야 하므로 서버는 **전체 인터페이스(0.0.0.0)** 에 바인딩합니다.
- 유선+무선이 섞이면 LAN IP 후보가 여러 개일 수 있어, 사설망 우선순위로 대표 IP를 고릅니다. QR이 안 되면 교사 화면의 접속 주소를 학생에게 직접 불러 주세요.

---

## 11. 데이터 저장과 백업

모든 데이터는 **exe 옆 폴더**에 파일로 저장됩니다(DB 없음).

```
classroom-quiz.exe
├─ quizzes/<퀴즈id>.json    ← 퀴즈 (문항·보기·그림이 한 파일에)
├─ results/<퀴즈id>/<날짜_시각>.json   ← 게임 결과 (퀴즈별·날짜별)
└─ mcp-clients.json                 ← [AI 연결]로 등록해 준 설정 파일 목록 (없어도 됨)
```

- **그림은 별도 파일이 아닙니다.** 퀴즈 JSON 안에 base64 문자열로 통째로 들어갑니다 → 파일 하나만 옮기면 그림째 따라갑니다.
- 저장은 임시파일→교체 방식이라 저장 중 꺼져도 파일이 깨지지 않습니다.

**백업**: `quizzes/`와 `results/` 폴더를 통째로 복사해 두면 됩니다. 다른 PC로 옮길 때도 이 폴더만 가져가면 그대로 복원됩니다.

---

## 12. 자주 묻는 질문

**Q. 설치가 필요한가요?**
아니요. `classroom-quiz.exe` 하나만 더블클릭하면 됩니다. 학생도 설치 없이 브라우저로 들어옵니다.

**Q. 인터넷이 안 되는 교실에서도 되나요?**
됩니다. 같은 와이파이(공유기/AP)에만 붙어 있으면 인터넷 없이 동작합니다. 웹 화면과 QR이 exe에 내장돼 있습니다.

**Q. 학생이 QR을 못 찍어요.**
교사 화면의 **접속 주소**(예: `http://192.168.0.x:8080/`)를 불러 주고 학생이 브라우저에 직접 입력하게 하세요. QR을 **전체화면으로 크게** 띄우면 멀리서도 잘 찍힙니다.

**Q. 학생이 안 보여요 / 접속이 안 돼요.**
① 학생이 **교사 PC와 같은 와이파이**인지, ② 헤더 배지가 **유선**이면 학생 무선망과 다른 망일 수 있으니 확인, ③ 처음 실행 때 **방화벽 허용**을 눌렀는지 확인하세요.

**Q. 그림은 어디에 저장되나요?**
퀴즈 JSON 파일 **안에** 저장됩니다(경로가 아니라 그림 자체). 그래서 `quizzes/` 폴더엔 `.json`만 보이고, 파일 하나만 공유하면 그림도 함께 전달됩니다.

**Q. 다른 선생님이 만든 퀴즈를 쓰려면?**
받은 `.json` 파일을 **↥ 가져오기**로 넣으면 됩니다. 항상 새 퀴즈로 추가되어 내 퀴즈를 덮지 않습니다.

**Q. 결과를 엑셀로 보고 싶어요.**
**📊 결과** → 해당 게임 펼치기 → **CSV 내보내기**. 엑셀에서 바로 열리고 한글도 깨지지 않습니다.

**Q. 진행 중에 처음으로 돌아가려면?**
왼쪽 위 **엠블럼**을 누르세요. 진행 중이면 확인 후 로비로 돌아갑니다.

**Q. 나간 학생 이름이 명단에 그대로 있어요.**
로비에서는 연결이 끊기면 **바로 사라집니다**(교사가 새로고침할 필요 없음). 게임 중에는 점수를
지키려고 남겨 두며, 그 학생이 같은 닉네임으로 다시 들어오면 남아 있던 자리를 이어받습니다.

**Q. 학생이 닉네임을 잘못 입력했어요.**
학생이 **자기 화면을 새로고침**하고 이름을 다시 넣으면 됩니다. 로비면 옛 이름은 사라지고,
게임 중이라도 **원래 열려 있던 화면에서** 다시 넣으면 점수가 그대로 유지됩니다.
(게임 중에 QR을 새로 찍어 **다른 이름**으로 들어가면 새 학생으로 잡혀 점수가 0부터입니다.)

**Q. 화면의 타이머 숫자가 왔다 갔다 해요.**
예전 버전의 증상입니다. 새 버전에서는 서버가 재는 남은 시간을 그대로 보여 주므로 숫자가 튀지
않습니다 → [13. 준비 0단계](#13-ai로-퀴즈-만들기-mcp)처럼 새 exe로 덮어써 주세요.

**Q. 문항을 일일이 타이핑하기 힘들어요. AI에게 시킬 수 있나요?**
됩니다. exe 하나로 AI(예: Claude)와 연결해 초안을 받고, 편집기에서 다듬어 쓰면 됩니다.
설치할 프로그램은 따로 없습니다 → [13. AI로 퀴즈 만들기](#13-ai로-퀴즈-만들기-mcp)

---

## 13. AI로 퀴즈 만들기 (MCP)

문항을 하나씩 타이핑하는 대신, **AI에게 초안을 맡길 수 있습니다.**
"5학년 2학기 분수의 나눗셈으로 10문항 만들어줘" 라고 말하면 AI가 퀴즈를 만들어
`quizzes/` 폴더에 **바로 저장**합니다. 교사는 편집기에서 확인·수정한 뒤 그대로 진행하면 됩니다.

**추가 설치는 없습니다.** 갖고 계신 `classroom-quiz.exe` 자체가 AI와 대화하는 창구
(MCP 서버)입니다. 파이썬이나 Node 같은 프로그램을 따로 깔 필요가 없습니다.

### 준비 (한 번만)

0. **exe가 최신 버전인지 확인합니다.** 이 기능은 나중에 추가되었으므로, 예전에 받은
   exe에는 없습니다. 새로 받은 `classroom-quiz.exe` 로 **파일만 덮어쓰면** 됩니다 —
   같은 폴더의 `quizzes/`·`results/` 는 건드리지 않으므로 **기존 퀴즈와 기록은 그대로**입니다.
   (앱이 켜져 있으면 먼저 닫고 덮어쓰세요.)
1. AI 프로그램을 설치하고 로그인합니다. (**Claude Desktop**, **Claude Code**, **Cursor** 등)
2. AI 프로그램을 **완전히 종료**한 뒤, 교실 퀴즈를 실행하고 교사 화면 오른쪽 위 **[AI 연결]** 을 누릅니다.
   "**Claude Desktop 에 연결했습니다**" 처럼 **어디에 연결했는지** 알려 줍니다. 설정 파일을 열어
   경로를 적을 필요가 없습니다.
   - 이 PC에 **깔려 있는 AI 프로그램만** 찾아 등록합니다(여러 개면 여러 개 모두).
   - "AI 프로그램을 찾지 못했습니다"라고 나오면 아직 설치가 안 된 것입니다. 설치한 뒤 다시 누르세요.
     (안 깔린 프로그램의 설정 파일을 미리 만들어 두지는 않습니다.)
3. AI 프로그램을 다시 켭니다. 도구 목록에 `classroom-quiz` 가 보이면 준비 끝입니다.

> **새 버전을 받아도 다시 연결할 필요가 없습니다.** 내려받은 파일 이름에는 버전과 번호가
> 붙어 매번 달라지는데(예: `교실_퀴즈-0.0.0-1fb490d.exe`), 새 exe 를 **한 번 실행하면**
> 프로그램이 연결 경로를 스스로 새 파일로 맞춥니다. exe 를 다른 폴더로 옮겼을 때도 같습니다.

**[AI 연결]이 못 찾는 프로그램을 쓴다면** — 다른 프로그램들도 대개 같은 형식(`mcpServers`)을
씁니다. 그 프로그램의 **설정 파일 경로를 한 번만 알려 주면** 됩니다(명령 프롬프트에서):

```
classroom-quiz.exe mcp-install "설정 파일 경로"
```

한 번 알려 준 파일은 **기억해 두었다가 다음부터 자동으로 갱신**합니다. 즉 어떤 AI 프로그램을
쓰든, 새 exe 를 한 번 실행하면 경로가 알아서 새 파일로 맞춰집니다.

> **등록은 그 AI 프로그램을 끈 상태에서** 하세요. 실행 중에 설정 파일을 스스로 다시 쓰는
> 프로그램이 있어, 켜 둔 채로 등록하면 방금 넣은 내용이 덮어써질 수 있습니다.
> 등록 뒤에는 그 프로그램의 도구 목록(또는 MCP 서버 목록)에 `classroom-quiz` 가 있는지 확인하세요.

직접 적어 넣어도 됩니다. `command` 에는 exe 의 전체 경로를, `\` 는 `\\` 처럼 두 번 씁니다:

```json
{
  "mcpServers": {
    "classroom-quiz": {
      "command": "C:\\Users\\사용자이름\\Desktop\\classroom-quiz.exe",
      "args": ["mcp"]
    }
  }
}
```

### 쓰는 법

- **만들기** — "3학년 곱셈 단원으로 8문항, 보기 4개씩 만들어줘"
- **고치기** — "방금 만든 퀴즈에서 3번 문항을 더 쉽게 바꿔줘"
- **보충 퀴즈** — "지난주 결과 보고 많이 틀린 내용으로 5문항 만들어줘"
  (AI가 `results/` 의 지난 게임 기록을 읽어 참고합니다.)
- **목록·삭제** — "내 퀴즈 목록 보여줘", "○○ 퀴즈 지워줘"

만들어진 퀴즈는 손으로 만든 퀴즈와 **완전히 같은 파일**입니다. 편집기에서 문항을 고치거나,
그림을 넣거나, 다른 선생님께 `.json` 으로 건네는 것 모두 그대로 됩니다.

### 알아 두면 좋은 점

- 만든 퀴즈는 교사 화면 목록에 **몇 초 안에 저절로 나타납니다**(새로고침 필요 없음).
- AI에게 부탁하려면 **인터넷이 필요합니다.** (수업 진행 자체는 인터넷 없이 됩니다 — 퀴즈만 미리 만들어 두세요.)
- AI는 사실을 틀릴 수 있습니다. **정답을 반드시 확인**한 뒤 수업에 쓰세요.
- 그림은 AI가 넣지 못합니다(그림은 편집기에서 직접 추가). AI가 기존 퀴즈를 고쳐도
  **이미 넣어 둔 그림은 그대로 유지**됩니다.
- 삭제는 되돌릴 수 없습니다. AI에게 삭제를 시킬 때는 퀴즈 이름을 정확히 확인하세요.
- 퀴즈는 **내 PC 안에서만** 저장·수정됩니다(서버 계정 없음). 다만 AI에게 보낸 대화 내용은
  해당 AI 서비스로 전송되므로, 학생 이름 같은 개인정보는 넣지 마세요.

### 잘 안 될 때

- **AI가 `classroom-quiz` 도구를 못 찾습니다.**
  ① 지금 쓰는 exe 로 **[AI 연결]** 을 한 번 더 누르고, ② AI 프로그램을 **완전히 종료**한 뒤 다시 켭니다.
  (창만 닫으면 트레이에 남아 있을 수 있습니다 — 작업표시줄 오른쪽 아이콘에서 종료.)
  ③ "설정 파일 형식이 올바르지 않다"고 나오면, 그 파일을 전에 손으로 고치다 깨진 것입니다.
  교실 퀴즈는 **다른 연결까지 지워질까 봐 건드리지 않고 멈춥니다** — 파일을 고친 뒤 다시 누르세요.
- **만들었다는데 목록에 안 보입니다.** 목록은 몇 초마다 저절로 갱신되니 잠깐 기다려 보세요
  (편집기 화면은 창을 한 번 클릭하면 갱신됩니다). 그래도 없으면 exe 옆 `quizzes/` 폴더에
  `.json` 이 늘었는지 확인합니다. 파일이 있는데 안 보이면 **다른 폴더의 exe**를 실행하고
  있는 것입니다 — 지금 쓰는 exe로 **[AI 연결]** 을 다시 누르세요.
- **AI 쪽에 `classroom-quiz` 가 빨간색(오류)으로 뜹니다.** exe를 지웠거나 다른 곳으로 옮겼을 때입니다.
  지금 쓰는 exe를 한 번 실행하면 연결이 자동으로 새 위치를 가리킵니다.
- **"각 문항에 정답을 1개 이상 표시하세요" 같은 말이 나옵니다.** 편집기와 같은 검사 규칙이라
  AI가 스스로 고쳐 다시 저장합니다. 반복되면 "보기는 4개, 정답 1개로 만들어줘" 라고 알려 주세요.

---

문의·개선 요청은 저장소 이슈로 남겨 주세요.
